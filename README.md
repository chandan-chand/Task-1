# Task-1
End-to-end data science project that predicts student performance using machine learning, including data preprocessing, model training, and evaluation. The trained model is deployed as a REST API using Flask, enabling real-time predictions through user input.
